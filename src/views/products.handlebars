<h1>Bienvenido {{ user.email }}</h1>
{{#if products.length}}
    {{#each products}}
        <ul>
            <li>{{title}}</li>
        </ul>
    {{/each}}
    {{else}}
        <p>No hay productos</p>
{{/if}}